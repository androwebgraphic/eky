# Copy this file to .env and set a working MONGO_URI
# For a local MongoDB server use something like:
# MONGO_URI=mongodb://localhost:27017/eky

# For MongoDB Atlas use the connection string provided by Atlas
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xyz.mongodb.net/eky?retryWrites=true&w=majority

# Optional: set client origin for CORS
# CLIENT_ORIGIN=http://localhost:3000

# API base for absolute URLs (e.g. http://localhost:3001 or http://yourhost:3001)
API_BASE=http://localhost:3001

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_here

# Email Configuration (for password reset functionality)
# For Gmail, use your email and app password
# To get Gmail app password: https://support.google.com/accounts/answer/185833
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password

# Client URL (for password reset links)
CLIENT_URL=http://localhost:3000

# Note: For production, use secure values and proper email service like SendGrid or AWS SES
